fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9,<1.0
# Dependencies used by the metadata, QR, and ink analyzers exposed by this API.
PyMuPDF>=1.24.0
numpy>=1.26.0
opencv-python>=4.10.0,<5.0
zxing-cpp>=2.2.0
Pillow>=10.0.0
scipy>=1.11.0
pypdf>=4.0.0
matplotlib>=3.8.0
pdfplumber>=0.11.0
pytesseract>=0.3.10
openpyxl>=3.1.0
# Enables face-identity matching across a batch's document photos (see
# backend/photo_identity.py). Without these, extract_photo.py falls back to a
# plain OpenCV cascade that has no identity embeddings, and batch photo
# duplicate detection silently reports nothing rather than failing.
insightface>=0.7.3
onnxruntime>=1.17.0

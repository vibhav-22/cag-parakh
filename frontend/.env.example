# Leave NEXT_PUBLIC_API_URL empty to proxy /api through the frontend origin
# (required for the access-code cookie). Set BACKEND_URL if the FastAPI
# service runs somewhere other than http://127.0.0.1:8000.
NEXT_PUBLIC_API_URL=
BACKEND_URL=http://127.0.0.1:8000
